import React from 'react';
import Table from '../../components/Table';

export default function index() {
    return (
        <div>
            <Table />
            <Table />
        </div>

    );
}
